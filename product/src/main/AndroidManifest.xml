<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.product.com">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".mvc.ui.SearchBaseActivity" />
        <activity android:name=".mvp.ui.ProductDetailActivity" />
        <activity android:name=".mvc.SearchResultListActivity" />
        <activity android:name=".mvc.ui.SelectProductActivity"/>
        <activity android:name=".mvp.ui.ShareSearchActivity"/>
        <activity android:name=".mvc.ui.PdfActivity"/>
    </application>

</manifest>