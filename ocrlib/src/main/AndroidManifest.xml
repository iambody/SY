<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="app.ocrlib.com">

    <application
        android:allowBackup="true"
        tools:replace="android:allowBackup"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".cardphoto.CardCameraActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".cardphoto.TestCardCamera" />
        <activity android:name=".facepicture.FacePictureActivity" android:screenOrientation="portrait" />
        <activity android:name=".identitycard.IdentityCardActivity" android:screenOrientation="portrait" />
        <activity android:name=".identitycard.IdentityCardTest"/>
    </application>

</manifest>
